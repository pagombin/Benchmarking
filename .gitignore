__pycache__/
*.pyc
*.egg-info/
.venv/
.mypy_cache/
.pytest_cache/
results/
build/
dist/

# frontend build tooling (the *built* SPA under src/pgbench_webapp/static/spa is
# committed so the package ships ready-to-serve assets — only node_modules is ignored)
frontend/node_modules/
frontend/.vite/

# web app local data
*.db
*.db-*
secrets.enc
secret.key
certs/
frontend/*.tsbuildinfo
